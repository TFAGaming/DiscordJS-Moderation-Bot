const { } = require('discord.js');

module.exports = {
    name: '',
    description: '',
    type: 1,
    options: [],
    role_perms: [String],
    developers_only: Boolean,
    owner_only: Boolean,
    category: String,
    run: async (client, interaction, config) => {
        
    }
};